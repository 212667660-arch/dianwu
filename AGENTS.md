# 软件杯项目协作规则

本规则适用于 E:\软件杯 目录及其所有子目录中的 Codex 对话。

## 模型使用建议

- Terra：作为主力后端开发模型，负责日常 CRUD、业务逻辑、FastAPI 接口、数据处理、常规调试与测试编写。
- Sol：用于复杂架构设计、多 Agent 协议设计、流式输出方案、疑难 Bug 排查、关键技术选型、跨模块重构与高风险代码审查。
- Luna：用于轻量小工具、注释补充、README 和文档编写、简单摘要、格式整理与低复杂度辅助任务。

## A3 项目默认策略

对于“基于大模型的个性化资源生成与学习多智能体系统开发”项目，默认建议使用 Terra；遇到系统架构、多智能体通信协议、复杂异步流程或难以定位的错误时，建议切换到 Sol；编写文档、注释和简单脚本时，建议使用 Luna。

## 模型任务队列执行规则

- 新对话开始时，先读取 E:\软件杯\codex\AI模型任务队列.md。
- 收到新任务后，先登记目标模型、优先级、依赖和状态。
- 当前模型优先批量处理其“就绪”任务，不属于当前模型的任务保留到对应队列。
- 只有当前批次完成、全部阻塞或存在关键依赖时，才提醒用户切换模型。
- 用户手动切换模型后，更新队列中的“当前模型批次”，再继续对应任务。
- 每完成一个任务，立即更新任务状态、修改文件和验证结果。
- 不得为清空队列而跳过依赖、测试或用户确认的破坏性操作。

## 回答结束提醒

回答结束时简要报告当前模型批次、已完成任务和下一项任务。仅当当前模型队列已完成、被阻塞或继续工作需要其他模型时，才明确提醒用户切换模型并说明原因。

模型切换仍由用户在界面中手动完成；项目任务状态以 codex/AI模型任务队列.md 为准。
